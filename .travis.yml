language: go
go:
  - 1.12.x
install:
  - go get -u -v github.com/gohugoio/hugo
deploy:
  provider: pages
  skip_cleanup: true
  GitHub_token: $GITHUB_TOKEN
  local_dir: public
  on:
    branch: master

